import { APP_NAME } from "@vms/domain";
import { AppShell } from "@vms/ui";

export default function App() {
  return (
    <AppShell title={`${APP_NAME} — Vendor Portal`}>
      <p>Phase 0 scaffold. Registration and status screens land in later tickets.</p>
    </AppShell>
  );
}
