{
  "name": "@vms/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Soechi VMS — Drizzle schema & Postgres client (Phase 0). See ../../docs/adr.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "seed": "bun run src/seed.ts",
    "test": "bun test",
    "push": "drizzle-kit push",
    "studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "drizzle-kit": "^0.28.1",
    "typescript": "^5.6.3"
  }
}
